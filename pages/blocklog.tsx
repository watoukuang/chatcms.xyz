import React from 'react';
import BlocklogView from '@/src/views/blocklog';
import Layout from '@/src/layout';

export default function BlocklogPage() {
    return (
        <Layout>
            <BlocklogView />
        </Layout>
    );
}
